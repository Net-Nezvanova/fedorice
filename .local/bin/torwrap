#!/bin/sh

ifinstalled transmission-remote transmission-daemon || exit 1

! pidof transmission-daemon >/dev/null && transmission-daemon && notify-send "Starting torrent daemon..."

$TERMINAL -e watch -n 5 transmission-remote -l; pkill -RTMIN+7 "${STATUSBAR:-dwmblocks}"
